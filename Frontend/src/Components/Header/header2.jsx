import img from '../../images/aboutUs_manager.png'

const Header2 =()=>{
    return (
        <header className="header-aboutUs" style={{backgroundImage: `url(${img})`}}>
            <h2>Proč realizovat inventuru s InvenT genius</h2>
        </header>
    )
}

export default Header2;